---
name: source-command-harness-health
description: Diagnose, validate, prepare, and evaluate app-factory handoffs across Codex, Claude, and CLI agents. Use when tool or receipt JSON is malformed, a context capsule is stale, an agent switch loses task state, a handoff is blocked or degraded, the user says an agent forgot the task, or app-factory harness compatibility needs verification.
---

# Verify harness health

Diagnose, validate, prepare, and evaluate app-factory handoffs across Codex, Claude, and CLI agents.

Read `.claude/commands/harness-health.md` in this repository and follow it verbatim; it is the
canonical procedure for this workflow.

Portable invocation: `$source-command-harness-health`.
