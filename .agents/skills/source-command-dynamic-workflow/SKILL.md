---
name: source-command-dynamic-workflow
description: Plan and run a provider-adaptive dynamic workflow for complex work that divides into independent research, review, migration, classification, or verification tasks. Use when the user explicitly asks for a workflow, large fan-out, many independent checks, adversarial verification, a tournament, or a large migration.
---

# Dynamic workflow

Plan and run a bounded, provider-adaptive multi-agent workflow.

Read `.claude/commands/dynamic-workflow.md` in this repository and follow it verbatim; it
is the canonical procedure for this workflow.

Portable invocation: `$source-command-dynamic-workflow`.
