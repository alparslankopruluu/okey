# Decisions — ADR-lite log

*One line per decision that future-us might question. Newest on top. Also the ledger for experiments (hypothesis → result).*

| Date | Decision | Why | Rejected alternatives |
|---|---|---|---|
| 2026-08-13 | Bind command IDs to authoritative sequence and action kind, and allow multiple validated device sockets for one account to share exactly one room seat with full-snapshot reconnect | Prevents route remounts from reusing a counter-derived ID for another payload while preserving deterministic replay; two devices must never create two players for one account | Component-lifetime counters, allocating another seat, or silently making the newest socket invalidate the other before provider presence/session policy exists |
| 2026-08-13 | Carry allowlisted 100/500/1,000 room entries into exact mock settlement, persist gift receipts and settled match IDs across remounts, and accept wall drag only toward the responsive rack drop direction | Displayed room cost, economy result, social limits, and tactile feedback must stay truthful across navigation and orientation changes | Mapping all paid rooms to 100, resetting mock limits on screen mount, any-direction draw gestures, or using local state to authorize production wallet writes |
| 2026-08-12 | Use RN Firebase 26 modular app/auth/firestore/functions/App Check/messaging as the only mobile push/provider stack; keep permission education separate from token registration, and test rules in `demo-luma-okey` with no project alias | Avoids deprecated namespace API and duplicate token paths while preserving contextual permission UX, App Check enforcement, server-only social/economy writes, and fail-safe local proof | Adding expo-notifications, committing native config, client wallet writes, remote deploy during scaffolding, or claiming simulator APNs proof |
| 2026-08-12 | Use three local expo-audio channels with reproducible FFmpeg-synthesized effects, speech-free seeded ambience, PTT duck and background pause; model gifts/friends/notifications behind authority-shaped local services before Firebase | Gives simulator-verifiable sound/social UX without copyright risk, provider credentials, duplicate push stacks, client-authored wallet totals, or a recording path | Scraped cafe audio, intelligible background speech, expo-notifications alongside FCM, gift value transfer, SDK calls directly from screens |
| 2026-08-11 | Adopt source-locked `classic-standard-v1` and `101-fixed-open-v1` settlement, deterministic exact-cover winner discovery, physical `TableMeld` ownership, and automatic-but-authoritative bot/user finish/open/layoff commands | Okey house scoring varies; naming the profile makes behavior testable and room-visible, while one solver/reducer prevents UI and bots from inventing different legal outcomes or losing tiles during 101 mutation | Calling one house rule universal, trusting caller-supplied winning melds, UI-only per displays, destructive rack removal without table ownership, or scoring only in presentation code |
| 2026-08-11 | End a round as a draw on the final discard after the wall is exhausted, persist an explicit finish reason, and prove the whole loop with a bounded deterministic four-bot runner | Prevents the next player from getting stuck on `source_empty`, gives replay/persistence an unambiguous terminal state, and closes full-round execution without inventing unsettled scoring rules | Leaving the game stuck, declaring a winner without a legal finish, or guessing Classic/101 score settlement in the same change |
| 2026-08-11 | Keep Expo CNG native projects local/ignored, record simulator evidence separately, and persist offline matches in a versioned identity-checked envelope after hydration completes | Avoids committing regenerated Pods/Xcode output, prevents the initial render from overwriting a saved match, rejects corrupt/cross-seed snapshots, and preserves reproducible native proof | Committing generated native trees, trusting an unchecked JSON cast, or saving the fresh match before AsyncStorage hydration |
| 2026-08-11 | Use one SQLite-backed Durable Object per online room, typed RPC for room operations, hibernatable WebSockets for state broadcast, full-snapshot reconnect, and a 24-hour expiry alarm; keep deploy and Firebase token verification gated | Matches the game’s coordination atom, provides server-side turn/sequence/idempotency authority, and stays locally testable without credentials or spend | Client-authoritative multiplayer, a global room singleton, non-hibernating sockets, pretending the local identity header is production auth, or deploying without approval |
| 2026-08-11 | Implement provider boundaries as executable mocks with append-only/idempotent economy, 24-hour chat TTL/rate/block controls, and voice state that cannot record | Lets product flows and abuse invariants be tested before accounts, catalogs, legal review, and credentials exist | SDK calls from screens, writable wallet totals, fake provider proof, or silently enabling paid/stake flows |
| 2026-08-11 | Anchor the visual system to five original OpenAI-generated concept sheets, curate light/dark rooms and twelve avatar crops, and keep gameplay glyphs code-drawn | Establishes one coherent luminous-clay direction before feature code while preserving tile readability and provenance | Copying supplied screenshots, AI-rendered numbers/symbols, or mixing unrelated generated styles |
| 2026-08-11 | Name the working product Luma Okey; treat the basic App Store collision search as a risk screen, not trademark clearance | The luminous-calm direction is distinctive and directly informs the visual system | Niva Okey, Masa Nova, or claiming legal clearance |
| 2026-08-11 | Ship V1 mobile-only on Expo SDK 57 with a pure TypeScript game core; defer playable web to V2 | Native mobile interaction, voice, haptics, and store flows get the strongest first implementation while preserving reuse | Shipping three clients at once or coupling rules to React Native |
| 2026-08-11 | Use Reanimated + Skia 2.5D rather than a full Three.js mobile scene | Predictable gestures, readability, accessibility, and lower-device fallback matter more than free-camera 3D | Full 3D table on mobile; static flat UI |
| 2026-08-11 | Keep providers mock-first and all real Firebase/Cloudflare/RealtimeKit/RevenueCat/store mutations as explicit human TODOs | The user requested an implementable foundation without credentials, billing, deploy, or store side effects | Silent provider creation or fake integration evidence |
| 2026-08-11 | Separate chip economy from fairness; disable purchased-chip stake rooms in production pending review | Avoid pay-to-win/gambling ambiguity and protect trust | Cash/prizes/transfers, random paid items, or always-on stake rooms |
| 2026-08-11 | Add provider-adaptive Dynamic Workflows as an explicit capability with WorkflowPlan v1, normal 3-worker delegation, dynamic 8-concurrent/20-total defaults, exact scope/spend approval above 20, worktree-isolated writers, and CompletionReceipt aggregation; pin the Anthropic cookbook as doctrine only | Large independent research, verification, tournament, and migration work benefits from clean contexts and deterministic orchestration, while normal tasks should not pay the cost or accept expanded authority | Advertising 1,000 agents as a default/guarantee, vendoring Anthropic code, shared-file parallel writes, provider-specific contracts, or treating ChatGPT chat as a programmable fan-out runtime |
| 2026-08-11 | Adopt two knowledge-graph-cookbook methods as playbook doctrine: the extract→resolve→rank→cite review-mining pipeline (product-strategy/post-launch) and schema-guaranteed structured outputs plus two-tier/batch/caching cost shape for AI features (ai-media); add the pinned `web-3d` capability with three audited external Three.js skill packs | The methods raise evidence quality and app margins with zero new infrastructure, and 3D/browser-game work had no routed capability at all | Building a knowledge-graph store, NetworkX/Neo4j dependency, or multi-hop QA layer over kit docs (violates the no-embedding/no-RAG doctrine); vendoring the Three.js skills into the kit instead of pinning upstream |
| 2026-08-11 | Adopt the consumer-app operator doctrine (2026 practitioner guides) as kit defaults: tarpit/clone screen as a critical-risk trigger, narrow-niche rule, gotcha-moment canonical definition, v1 screen-count declaration in the blueprint, viral-acquires/useful-retains sequencing, charge-from-day-one, MRR ladder with don't-skip rungs, channel-decay doctrine, distribution playbook (creators/meme pages/UGC/paid ads), retention-over-acquisition priority in growth_diagnose | The kit's mechanical gates were strong but silent on ideation heuristics, feature-sequencing, and channel operations — the layers where solo founders actually stall; doctrine is pinned by tests so rewrites can't drop it | Importing the guides' rejected tactics (planted/seeded comments, onboarding rating screens, $9.99-monthly anchor, payoff-withholding without a truthful preview), a 9th score dimension, a new lifecycle stage or slash command |
| 2026-08-10 | Adopt asc 3.x: supported range `>=3.1.0,<4`, rorkai skills pinned to HEAD `13861c20` after scoped diff audit; new promo-image delete commands added to the settings ask list | asc 3.x improved the kit's own posture (file-driven batch mutations now require `--confirm`, HTTP-status exit codes, hardened install checksums) and 3.1.0 is the API 4.4.1 sync the audited skills document; audited skill diffs were defensive (no-delete workflows, web-session vs public-API separation, manual fallbacks) with no authority-expanding changes | Staying on 2.6.x forever (upstream skills no longer document it), jumping the floor to latest 3.7.0 (forces upgrades without a behavioral need) |
| 2026-08-10 | Pin refresh after diff audit: rorkai ASC skills → `0ae3da2b` (last pre-asc-3.0 commit only), google android/skills → `v1.0.7`/`1e5e7ae6`, rcosteira79 → `6373e59c`. All audited diffs were doc syncs, validator gates, and content fixes; no authority-expanding or injection-shaped instruction changes | rorkai HEAD targets asc 3.x while the kit deliberately supports `asc >=2.6.1,<3` ("do not install a future major"); partial advance takes the safe improvements (idempotent `subscriptions setup --repair`, strict validate gates) without the major-version jump | Bumping rorkai to HEAD (would document asc 3.x behavior the kit cannot run), leaving all three pins stale |
| 2026-08-10 | Kit 4 removes dead contract machinery (HarnessManifest, PortableEnvelope, CorrectionRequest, capsule telemetry, executionClass fields) and v1 run/receipt paths; the android lane stays evidence-light but requires recorded evidence to succeed | Ceremony that no workflow consumed hid the real guarantees (scope checks, digests, receipts, redaction); v2-only state removes silent migrations | Keeping unused schemas versioned forever, automatic correction retries, migrating v1 states in place |
| 2026-08-10 | Keep harness contracts provider-neutral, closed, versioned, and standard-library validated (superseded in kit 4: the digest-bound correction flow was removed) | Makes Codex/Claude/CLI handoffs comparable without adding a runtime dependency or pretending to control hidden harness schemas; preserves fail-closed mutation approvals | Persona-heavy prompts, tolerant unknown-field dropping, unlimited retries, provider SDK coupling, Pydantic/jsonschema runtime dependency |
| 2026-07-17 | Keep the status UI shell-free but allow locked atomic capability-queue writes; vendor the pinned HyperShots runtime inside store-assets and gate final creative after M1/M2 acceptance | Makes every ability discoverable and schedulable without turning the window into an external controller; preserves upstream screenshot power offline while retaining ASC approvals and launch-locale control | Prompt-only UI, shell-running dashboard, floating `npx` dependency, partial renderer rewrite, early store creative |
<!-- Example: | 2026-07-08 | zustand over Redux | 1-dev team, minimal boilerplate | Redux Toolkit, MobX | -->
| 2026-08-13 | Keep Luma midnight/pearl as the default and add Kahvehane as an optional visual-only atmosphere using an original generated walnut rack, warm table treatment, and deterministic white tile faces | The user wants both a distinctive premium identity and a familiar authentic coffeehouse option; separating visual atmosphere from rules preserves fairness/readability and avoids competitor trade dress | Replacing Luma globally, copying green/casino tables, or rasterizing gameplay glyphs |
| 2026-08-13 | Room discovery may filter by player level and free/mock-chip entry tier, while purchased-chip production stakes remain disabled | It improves progression and room fit without implying cash value or expanding the existing legal/store gate | Cash rooms, transfers, prizes, hidden stake, or client-authoritative connected wallet |
| 2026-08-13 | Bot turns should be deliberately readable and draw/rack arrangement should support drag-and-drop with accessible tap fallbacks | The table's tactile clarity is the core product wedge; instantaneous bots and tap-only manipulation weaken comprehension | Artificial delays longer than a natural human beat, drag-only controls, or motion without Reduced Motion equivalence |
